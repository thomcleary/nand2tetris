// This file is part of www.nand2tetris.org
// and the book "The Elements of Computing Systems"
// by Nisan and Schocken, MIT Press.
// File name: projects/11/Seven/Main.jack
/**
 * Computes the value of 1 + (2 * 3) and prints the result
 * at the top-left of the screen.  
 */
class Main {
   // TODO: remove class vars (only added for testing while building symbol table)
   field int a, b, c;
   static bool d;
   field Test e;
   static Array f, g;

   // TODO: remove parameters (only added to test symbol table)
   function void main(int h, int i) {
   // TODO: remove var declarations (only added to test symbol table)
      var int j;
      var Test k, l;

      do Output.printInt(1 + (2 * 3));
      return;
   }

}
